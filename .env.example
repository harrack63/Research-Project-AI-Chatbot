# IMPORTANT: THE BACKEND URL NEEDS TO RUN THROUGH /healthChatbot-backend FOR PROPER ROUTING. 
# Currently it doesnt, so we use the local IP address directly. Otherwise, all the parts work correctly and as intended. 

# NEXT_PUBLIC_API_URL ="https://hcss.cs.purdue.edu/healthChatbot-backend" # Production backend URL